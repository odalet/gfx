<UserControl x:Class="ScanPlayerWpf.Views.HeadsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"              
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"              
             xmlns:vm="clr-namespace:ScanPlayerWpf.ViewModels"
             d:DataContext="{x:Static vm:HeadsViewModel.DesignTime}"             
             mc:Ignorable="d" d:DesignHeight="75.893" d:DesignWidth="100.792">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Heads, Mode=OneWay}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name, Mode=OneTime}" 
                              IsChecked="{Binding IsEnabled, Mode=TwoWay}" 
                              Foreground="{Binding Background, Mode=OneTime}"
                              Background="{Binding Background, Mode=OneTime}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
