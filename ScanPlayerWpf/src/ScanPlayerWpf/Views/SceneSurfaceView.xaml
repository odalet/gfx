<UserControl x:Class="ScanPlayerWpf.Views.SceneSurfaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hx="http://helix-toolkit.org/wpf/SharpDX"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <hx:Viewport3DX x:Name="viewport3DX" />
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="Redraw" Click="RedrawButton_Click" Width="60" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" />
            <Button Content="2D" Click="OrthoButton_Click" Width="60" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" />
            <Button Content="3D" Click="PerspectiveButton_Click" Width="60" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" />
        </StackPanel>
    </Grid>
</UserControl>
